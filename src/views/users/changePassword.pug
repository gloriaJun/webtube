extends ../layouts/main.pug

block content
    .form--container
        form(action=`${routes.users}${routes.userChangePassword}`, method="post")
            input(type="password", name="currentPassword", placeholder="Input Current Password")
            input(type="password", name="password", placeholder="Input New Password")
            input(type="password", name="password2", placeholder="Verify Password")
            input(type="submit", value="Change Password")
